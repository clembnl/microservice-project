apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      initContainers:
        - name: wait-for-mongo
          image: busybox
          command: ['sh', '-c', 'until nc -z mongo-users.microservices.svc.cluster.local 27017; do echo waiting for mongo-users; sleep 10; done']
      containers:
        - name: users-service
          image: clementbonal/microservice-project_users-service:v1.1
          ports:
            - containerPort: 3001
          env:
            - name: MONGO_URI
              value: mongodb://mongo-users:27017/users_db
            - name: KAFKA_BROKER
              value: kafka:9092
          volumeMounts:
            - name: kafka-client-secrets
              mountPath: /etc/kafka/secrets
              readOnly: true
      volumes:
        - name: kafka-client-secrets
          secret:
            secretName: users-service-kafka-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: microservices
spec:
  ports:
    - port: 3001
  selector:
    app: users-service